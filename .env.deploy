# Deployment env vars for docker-compose.deploy.yml
# Copy or edit values before use

# Postgres
DB_NAME=stock_db
DB_USER=stock_user
DB_PASS=stock_pass

# Provider API keys
MARKETSTACK_API_KEYS=replace_with_real_keys

# Git source for app image build
GIT_REPO=https://github.com/IAteNoodles/stock_docker.git
GIT_REF=master
